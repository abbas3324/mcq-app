Kids MCQ Test App

Local dev scaffold for a kids-focused MCQ test app.

Run backend and frontend separately.

Backend:

```bash
cd backend
npm install
npm run dev
```

Frontend:

```bash
cd frontend
npm install
npm run dev
```

The backend provides simple endpoints to upload a `.docx` (or text) word list and generate MCQs.
